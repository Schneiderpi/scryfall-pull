[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "scryfall_pull_schneiderpi"
version = "0.0.1"
authors = [
  { name="Example Author", email="author@example.com" },
]
description = "A small example package"
readme = "README.md"
requires-python = ">=3.9"
license = "Apache 2.0"

[project.urls]
Homepage = "https://github.com/Schneiderpi/scryfall-pull"